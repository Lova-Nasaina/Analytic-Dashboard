const Content = ({district, data}) => {
    if (!district) return <div style={{ padding: '20px' }}>Sélectionnez un district dans le menu latéral</div>;

    return ( 
        
     );
}
 
export default Content;